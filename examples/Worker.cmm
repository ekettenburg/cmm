class Worker;

fn slow(x: Int) -> Int {
    doubled = (x * 2);
    return doubled;
}

fn main() {
    j1 = run slow(10);
    j2 = run slow(21);
    a = wait j1;
    b = wait j2;
    sum = (a + b);
    Console.print("result = ");
    Console.println(sum);
}
